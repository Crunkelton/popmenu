import { EditableOnEscapeDirective } from './editable-on-escape.directive';

describe('EditableOnEscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableOnEscapeDirective();
    expect(directive).toBeTruthy();
  });
});
